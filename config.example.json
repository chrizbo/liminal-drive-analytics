{
  "_comment": "Copy this file to config.json and edit to match your org's tools. config.json is gitignored.",
  "path_significant_domains": [
    "github.com",
    "arxiv.org",
    "amazon.com",
    "reddit.com",
    "goodreads.com"
  ],
  "openai_model": "gpt-5.4-mini",
  "_environment": {
    "OPENAI_API_KEY": "Optional. Enables Leader Brief polishing.",
    "DRIVE_ANALYTICS_WRITE_TOKEN": "Optional for local use. When configured, required for FastAPI POST/PATCH endpoints.",
    "DRIVE_ANALYTICS_CORS_ORIGINS": "Comma-separated origins; defaults to * for local use."
  }
}
